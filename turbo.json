{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "dev:storybook": {
      "dependsOn": ["^dev:storybook"],
      "cache": false
    }
  }
}
